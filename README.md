# Sywth's Notes

Personal notes, rendered with LaTeX.

## Setup (Debian Only)

Install `just` (Debian/Ubuntu):
```bash
sudo apt install just
```

## Commands

- `just` : Compile the full document and copy the PDF to `notes-rendered/`
- `just clean` : Remove all build artifacts
