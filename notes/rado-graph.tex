\documentclass[../main.tex]{subfiles}
\begin{document}

\question{Given some graph $G \coloneqq (V,E)$ where $V \subseteq \N$ and $E \subseteq V^2$ prove that for any infinite subset (where $\lvert V' \rvert = \aleph_0$ and $E' \subseteq {V'}^2$) $G' \coloneqq (V', E')$ contains $G$.}

Now first we need be far more strict here cause in all honesty I framed that question very poorly.

\end{document}
